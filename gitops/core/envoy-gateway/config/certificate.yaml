apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-brunosabenca-com
spec:
  dnsNames:
    - "*.brunosabenca.com"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: wildcard-brunosabenca-com-tls-cert
