apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-nce-wtf
spec:
  dnsNames:
    - "*.brunosabenca.com"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: wildcard-nce-wtf-tls-cert
